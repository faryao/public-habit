# Use Markdown as the system of record

Habit definitions and dated proof records will be stored as Markdown files in the public GitHub repository, following the editing pattern established by `public-diary`. This keeps the application on GitHub Pages without tokens, Issues, a database, or an external image service; the trade-off is that the Owner records evidence through GitHub's web editor rather than pasting directly into the rendered calendar.
