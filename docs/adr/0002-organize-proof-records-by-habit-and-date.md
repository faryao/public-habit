# Organize proof records by Habit and date

Each Habit is defined by `_habits/<habit-identifier>.md`, and each Proof Record is stored at `_proofs/<habit-identifier>/<YYYY-MM-DD>.md`. This makes every Habit–date pair independently editable through GitHub's web editor, avoids conflicts between Habits, and allows deleted Habit metadata to be recreated without losing its retained proof history.
